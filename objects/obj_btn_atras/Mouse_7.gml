/// @description Inserte aquí la descripción
// Puede escribir su código en est
if (room == rm_user_create){
	keyboard_key_press(vk_subtract)
	keyboard_key_release(vk_subtract)
}
else {

	keyboard_key_press(vk_backspace)
	keyboard_key_release(vk_backspace)
}